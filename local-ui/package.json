{
  "name": "local-ui",
  "version": "1.0.0",
  "type": "module",
  "description": "Local QA testing UI (proxy-only mode)",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env server.js",
    "prod": "node --env-file=.env.production server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
